{
  "name": "testgame",
  "version": "1.0.0",
  "description": "webTaleKit sample game",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "play": "rimraf dist && ls -1 ./src/scene/* | xargs -i{} npx wtc {} ./src/js/ && npx webpack serve --open --mode development",
    "dev": "rimraf dist && ls -1 ./src/scene/* | xargs -i{} npx wtc {} ./src/js/ && npx webpack serve --mode development",
    "build": "rimraf dist && ls -1 ./src/scene/* | xargs -i{} npx wtc {} ./src/js/ && webpack --mode production && rm -rf ./src/js/"
  },
  "author": "EndoHizumi",
  "license": "ISC",
  "devDependencies": {
    "filemanager-webpack-plugin": "^8.0.0",
    "rimraf": "^5.0.7",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.2",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.4"
  },
  "dependencies": {
    "html-webpack-plugin": "^5.6.0",
    "webtalekit": "file:../webTaleKit"
  }
}
